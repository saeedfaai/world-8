# ADR-0002 — Persistent Actor Identity

Status: ACCEPTED_FOR_REPO_BOOTSTRAP

## Decision
Actor identity is persistent and independent of provider/model/session. Each execution has a separate Execution Identity linked to the Actor.

Qualification and Authorization remain separate.
