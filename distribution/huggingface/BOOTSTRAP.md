# Hugging Face Bootstrap — World 8

Status: PREPARED / PROVIDER-SIDE CREATION PENDING

## Namespace
Preferred organization namespace: `world8`
Fallback: `world-8`
Do not register either as canonical until the exact created URL is verified.

## Create order
1. Create Hugging Face account if no verified personal account exists.
2. Create World 8 organization.
3. Add the prepared organization card from `ORGANIZATION_CARD.md`.
4. Create `world8-core` public repository and copy its prepared README.
5. Create `world8-forecast-hall` public repository and copy its prepared README.
6. Create `world8-market-data` public repository and copy its prepared README.
7. Create `world8-demo` as a **Static** public Space and copy `README.md` + `index.html` from its prepared folder.
8. Verify every surface links to canonical GitHub and the exact matching release.
9. Record all exact Hugging Face URLs in `EXTERNAL_LINKS.md` and Drive registries.

## Cost policy
Use Static Space for the initial public demo because it requires no compute. Do not introduce paid Gradio/Docker compute until there is a concrete runtime need and an explicit cost decision.

## Security
Never publish tokens, keys, cookies, private datasets, recovery links, production endpoints, trading credentials, or confidential runtime state.

## Canonicality
Hugging Face is an AI-native discovery/demo layer. GitHub remains canonical. Zenodo remains immutable scientific history.
